- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.config/nvim: nvim

- create:
    - ~/dotbot/downloads

- shell:
  - [git submodule update --init --recursive, Installing submodules]
